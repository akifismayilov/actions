name: Akif's action
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: First Step
        run : echo it's first

      - name: Second Step
        run : echo it's end
